# Agent Behavioral Instructions: Vernier

1. **Strictly Stay in Sandbox (`BypassSandbox: false`):** All commands must run inside the sandbox. Never trigger confirmation modals.
2. **Free Infrastructure Only:** Build exclusively as a static/serverless Next.js application for Vercel. Never spin up persistent daemon backends requiring paid VPS/Railway instances.
3. **Mandatory 3-Surface Architecture:**
   - Surface 1: High-Authority Front Door (Hero, 1-chrome header, economic grid)
   - Surface 2: Working Cockpit (Intent Charter, Vernier Radar, Bytecode Trace)
   - Surface 3: Cryptographic Proof Rail (SHA-256 State Root, EIP-712 Attestation)
4. **Visual & Design Discipline:**
   - Maintain Swiss Metrology aesthetic (obsidian slate `#0A0D14`, graphite `#0F172A`, calibrated amber `#F59E0B`, electric cyan `#06B6D4`, crimson alert `#EF4444`).
   - Calibrate against `winsznx` references before declaring complete.
   - Run the 7-Gate Visual Gauntlet Loop before handoff.
5. **English Only:** Output strictly in English across all thoughts, messages, and code.
